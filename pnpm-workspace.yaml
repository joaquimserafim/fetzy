# Standalone project. This file exists so pnpm stops walking up the directory
# tree and doesn't discover ~/pnpm-workspace.yaml (which would otherwise make
# pnpm treat fetchx as a workspace member of the home directory).
packages:
  - .
minimumReleaseAgeExclude:
  - '@joaquimserafim/ts-try@1.1.0'
